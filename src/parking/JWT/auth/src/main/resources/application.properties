# ══════════════════════════════════════════════
# application.properties — versión actualizada
# ══════════════════════════════════════════════

spring.datasource.url=jdbc:mysql://mysql_db:3306/authentication?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true

server.port=9000

# ── EMAIL (Gmail) ──────────────────────────────
# Reemplaza con tu correo y contraseña de aplicación de Gmail
# Para obtenerla: Google Account → Seguridad → Verificación 2 pasos → Contraseñas de app
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=pipecerquera123@gmail.com
spring.mail.password=xfrgckxgvlklaqmm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# URL base para el link del correo de verificación
app.base-url=http://localhost:9000